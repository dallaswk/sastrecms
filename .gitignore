# dependencies
node_modules/

# build output
dist/
.output/

# astro
.astro/

# env files (never commit secrets)
.env
.dev.vars
.env.local
.env.*.local

# cloudflare
.wrangler/

# local sqlite databases (create-site / local dev)
*.db
*.sqlite
*.sqlite3

# drizzle migrations (commit the SQL, not the journal metadata if not needed)
# drizzle/meta/

# OS
.DS_Store
Thumbs.db

# editors
.vscode/settings.json
.idea/

# bases de datos locales de inquilinos (provision --local)
.tenants/

# medios subidos en desarrollo (en producción van a R2)
public/uploads/
